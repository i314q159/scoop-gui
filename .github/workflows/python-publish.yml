name: i314q159

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: windows-latest 

    steps:
    - uses: actions/checkout@v2
    
    - name: Build
      shell: pwsh
      run: |
        pip install -r requirement.txt
        ./build.ps1
